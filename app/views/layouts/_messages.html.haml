.container
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      %div{ class: "alert alert-#{name.to_s == 'notice' ? 'success' : name.to_s}" }
        %button.close{ data: { dismiss: "alert" } } x
        = msg if msg.is_a?(String)
